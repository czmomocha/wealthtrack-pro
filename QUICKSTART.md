# 🚀 WealthTrack Pro - 快速开始

## 📦 一、本地开发（5分钟）

```bash
# 1. 安装依赖
npm install

# 2. 配置API Key（可选，用于AI分析功能）
cp .env.example .env.local
# 编辑 .env.local，添加 GEMINI_API_KEY

# 3. 同时启动前后端
npm run dev:all

# 访问：http://localhost:5173
```

---

## 🌐 二、VPS部署（3分钟）

### 准备工作
- 一台VPS（Ubuntu/Debian推荐）
- 已安装Node.js 18+
- 已安装Git

### 一键部署

```bash
# SSH登录VPS
ssh user@your-server-ip

# 克隆并部署
git clone https://github.com/czmomocha/wealthtrack-pro.git
cd wealthtrack-pro
chmod +x deploy-full.sh
./deploy-full.sh

# 完成！访问 http://your-server-ip:3000
```

### 开放端口

```bash
sudo ufw allow 3000/tcp
sudo ufw allow 3001/tcp
sudo ufw enable
```

---

## 💾 三、使用服务器同步（1分钟）

### 首次备份

1. 点击右上角「设置」
2. 找到「服务器数据同步」
3. 点击「备份当前数据」
4. **复制并保存生成的用户ID**（例如：`a3f2c8b9e1d4f5a7`）

### 其他设备同步

1. 在新设备打开应用
2. 进入「设置」→「服务器数据同步」
3. 输入你的用户ID
4. 点击「同步」

---

## 🔄 四、部署脚本说明

### `deploy-full.sh` - 完整部署（推荐）
- ✅ 前端 + 后端API
- ✅ 服务器端数据存储
- ✅ 自动配置环境变量
- ✅ PM2进程管理

### `deploy-simple.sh` - 简化部署
- ✅ 仅前端
- ❌ 无后端（使用jsonblob.com）

### `deploy.sh` - Nginx静态部署
- ✅ Nginx托管
- ✅ 生产级配置
- ❌ 需要手动配置Nginx

---

## 📱 五、功能清单

| 功能 | 说明 | 状态 |
|------|------|------|
| 多账户管理 | 创建多个钱包（如：美股、A股） | ✅ |
| 资产追踪 | 股票、房产、存款等 | ✅ |
| 多币种支持 | CNY/USD/HKD + 自定义 | ✅ |
| 收益计算 | 年化收益率、加权平均 | ✅ |
| 可视化图表 | 饼图、柱状图 | ✅ |
| AI投资分析 | Gemini AI智能建议 | ✅ |
| 服务器同步 | 跨设备数据同步 | ✅ |

---

## ⚙️ 六、环境变量配置

编辑 `.env.local`：

```bash
# Gemini API Key（用于AI分析）
GEMINI_API_KEY=your_key_here

# 后端API地址
VITE_API_URL=http://your-server-ip:3001/api

# 后端端口
PORT=3001
```

**获取Gemini API Key：**
访问 https://ai.google.dev/ 申请免费额度

---

## 🛠️ 七、常用命令

### 服务管理
```bash
pm2 status              # 查看状态
pm2 logs                # 查看日志
pm2 restart all         # 重启所有服务
pm2 stop all            # 停止所有服务
```

### 手动启动
```bash
# 后端
npm run start:server

# 前端（开发）
npm run dev

# 前端（生产）
npm run build
npm run preview
```

### 数据备份
```bash
# 备份用户数据
tar -czf backup-$(date +%Y%m%d).tar.gz user-data/

# 恢复数据
tar -xzf backup-20241231.tar.gz
```

---

## 🐛 八、故障排查

### 前端无法连接后端

**检查后端状态：**
```bash
curl http://localhost:3001/api/health
```

**预期响应：**
```json
{"status":"ok","timestamp":1735689600000}
```

### 同步失败

**常见原因：**
1. 后端服务未启动 → `pm2 restart wealthtrack-backend`
2. 防火墙阻止 → `sudo ufw allow 3001/tcp`
3. API地址错误 → 检查 `.env.local`

### 端口被占用

```bash
# 查找占用进程
lsof -i :3001

# 终止进程
kill -9 <PID>
```

---

## 📊 九、性能优化

### Nginx缓存配置

```nginx
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### Gzip压缩

```nginx
gzip on;
gzip_types text/css application/javascript application/json;
gzip_min_length 1000;
```

---

## 🔐 十、安全检查清单

- [ ] 修改默认端口（可选）
- [ ] 配置防火墙规则
- [ ] 启用HTTPS（Let's Encrypt）
- [ ] 定期备份 `user-data/` 目录
- [ ] 不要将 `.env.local` 提交到Git
- [ ] 限制API访问IP（可选）

---

## 📞 支持

- **文档：** [DEPLOYMENT.md](DEPLOYMENT.md)
- **问题反馈：** GitHub Issues
- **API文档：** `http://your-server:3001/api/health`

---

## 🎯 下一步

1. ✅ 本地测试所有功能
2. ✅ 部署到VPS
3. ✅ 配置HTTPS
4. ✅ 设置定期备份
5. ✅ 开始管理你的财富！

**祝你投资顺利！💰**
